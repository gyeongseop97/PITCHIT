import{integer,sqliteTable,text}from"drizzle-orm/sqlite-core";export const games=sqliteTable("games",{id:text("id").primaryKey(),code:text("code").unique(),status:text("status").notNull(),mode:text("mode").notNull(),homeId:text("home_id"),awayId:text("away_id"),homeName:text("home_name"),awayName:text("away_name"),inning:integer("inning").notNull().default(1),half:text("half").notNull().default("top"),outs:integer("outs").notNull().default(0),balls:integer("balls").notNull().default(0),strikes:integer("strikes").notNull().default(0),homeScore:integer("home_score").notNull().default(0),awayScore:integer("away_score").notNull().default(0),bases:text("bases").notNull().default("[0,0,0]"),homeChoice:text("home_choice"),awayChoice:text("away_choice"),lastPlay:text("last_play"),message:text("message").notNull().default("상대를 기다리는 중입니다."),updatedAt:integer("updated_at").notNull()});
