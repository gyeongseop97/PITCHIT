# PITCHIT 3D 업데이트 · 2026-09-16

이 소스에는 선수 모델 개선, 타격·투구 카메라, 경기장 확장과 진행 속도 조정이 모두 포함되어 있습니다. 게임 규칙·승패 판정·구매 데이터 처리 방식은 변경하지 않았습니다.

## 이번 변경

- 선수: 15개 관절을 사용하는 모델, 관절 주변의 부드러운 변형, 헬멧·글러브·포수 보호구, 손과 배트 연결 및 발 고정.
- 카메라: 타격/투구 역할에 따른 자동 전환. 투구 시 스트라이크존은 화면 높이에 맞춰 확대하며, 투구 중에는 시점을 고정합니다.
- 경기장: 양쪽 덕아웃과 벤치·장비, 홈 뒤 안전망, 측면과 후면 관중석, 지붕·통로·외벽, 불펜·조명탑·외야 안내판.
- 템포: 준비 동작 1.1초 → 0.86초, 포수까지의 마지막 이동 0.22초 → 0.13초, 일반 송구 준비 0.62초 → 0.38초. 투구 비행 시간은 기존 구속을 따릅니다.
- 공의 잔상은 끝으로 갈수록 흐려지며, 타격 이후 카메라는 더 빨리 전환하고 타구를 더 가까이 보여 줍니다.
- 경기 결과와 위치 기록은 공이 배트 또는 포수에게 도착한 뒤 표시됩니다.

## 소스 위치

| 파일 | 역할 |
| --- | --- |
| `public/3d/player-model.js` | 선수 형상, 뼈대, 유니폼과 장비 |
| `public/3d/rig-poses.js` | 손과 발의 위치를 맞추는 관절 계산 |
| `public/3d/stadium.js` | 경기장 시설, 관중석, 하늘 |
| `public/3d/camera-framing.js` | 화면 크기와 좌우타/좌우투에 따른 카메라 |
| `public/3d/presentation-timing.js` | 준비·포구·송구·전환 시간 |
| `public/3d/scene.js` | 게임 결과와 3D 장면의 연결 |
| `public/3d/defense.js` | 수비수 이동·포구·중계 송구 |
| `scripts/3d-entry.template` | 게임 내 3D 버튼, 카메라 메뉴와 선택 UI |

`public/`이 원본이고 `static/`은 GitHub Pages용 복사본입니다. 두 폴더를 따로 수정하지 말고 원본 수정 후 아래 명령으로 동기화하세요.

```sh
node scripts/build-static.mjs
```

## GitHub에 반영하기

1. ZIP을 해제하고 `PITCHIT` 폴더 안의 파일을 확인합니다.
2. 기존 저장소에 변경 내용을 반영합니다. 저장소 안에 `PITCHIT/PITCHIT`처럼 한 단계 더 중첩하지 마세요.
3. 서버용 환경 변수와 기존 배포 설정은 유지합니다. ZIP에는 `.git`, 설치된 패키지, 로컬 비밀 설정, 개발 중 캡처 파일이 포함되지 않습니다.
4. 기존 `.github/workflows/deploy-pages.yml`은 `main`에 변경이 올라오면 `static/`을 생성해 GitHub Pages에 배포합니다. GitHub의 Actions에서 배포 성공 여부를 확인하세요.
5. 배포 후 게임을 새로고침하고 3D 모드를 켜서 확인합니다. 대전 기능은 기존 연결된 서버를 계속 사용합니다.

이 전달본 자체는 원격 저장소에 업로드하거나 정식 사이트에 배포하지 않았습니다.

## 검증

Node.js 22.13 이상이 필요합니다. 브라우저 검증에는 Playwright와 Chromium 계열 브라우저가 필요합니다. 별도로 설치된 Playwright를 쓰는 환경에서는 `PLAYWRIGHT_MODULE`, Edge를 쓰는 환경에서는 `BROWSER_CHANNEL=msedge`를 지정할 수 있습니다.

```sh
node scripts/verify-3d-plans.mjs
node scripts/verify-3d-models.mjs
node scripts/verify-3d-stadium.mjs
node scripts/verify-3d-local.mjs
node scripts/verify-3d-browser.mjs
```

검증 항목: 좌우타·좌우투 4가지 조합, 모바일 25칸 선택, 투구 중 카메라 고정, 관절 변형과 발 접지, 타구별 도착 위치, 홈런의 담장 통과, 포구 전 결과 숨김, 기존 선택·대전 요청 전달 및 WebGL 오류.
